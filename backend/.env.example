# ── Seguridad ──────────────────────────────────────────────────────────────────
SECRET_KEY=cambia_esto_por_una_clave_segura_en_produccion
APP_VERSION=20250821-01
APP_TIMEZONE=America/Bogota

# ── MySQL (datos operacionales) ────────────────────────────────────────────────
DB_HOST=10.108.34.32
DB_PORT=33063
DB_NAME=ccot
DB_USER=ccot
DB_PASSWORD=ccot

# ── Caché en memoria para /api/datos (segundos) ────────────────────────────────
CACHE_DATOS_TIMEOUT=600

# ── Admin inicial (se crea automáticamente si no existe en SQLite) ─────────────
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ── Web Push VAPID (genera con: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print(v.public_key, v.private_key)") ──
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CLAIMS_EMAIL=admin@tecnicos-dashboard.local

# ── API Key para el bot de Telegram (sin sesión) ───────────────────────────────
API_BOT_KEY=
